'use strict';
// The marker scripts/lab-plugin-completion.js prints. Kept separate so a lab
// runner can match it without installing the wrapper in its own process.
module.exports = { MARKER: 'NSC_LAB_PERSISTENCE_COMPLETE' };
